name: Publicar no GitHub Pages

on:
  push:
    branches: [main]
  # Permite republicar sob demanda pela aba Actions.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Uma publicação por vez, sem cancelar a que já está no ar.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Gerar o site
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: npm

      - name: Instalar dependências
        run: npm ci

      # Falha aqui, e não no fim, quando o Pages ainda não foi ativado
      # em Settings → Pages → Source: GitHub Actions.
      - name: Conferir a configuração do Pages
        uses: actions/configure-pages@v6

      # O ID do cliente OAuth do Google entra aqui, no build, vindo das
      # "Variables" do repositório — nunca escrito no código. Sem ele o site
      # publica normalmente; só o botão do Google Drive fica desligado, com a
      # explicação na tela. Ver o LEIA-ME, em "Google Drive".
      - name: Build de produção
        run: npm run build
        env:
          VITE_GOOGLE_CLIENT_ID: ${{ vars.VITE_GOOGLE_CLIENT_ID }}
          VITE_GOOGLE_API_KEY: ${{ vars.VITE_GOOGLE_API_KEY }}
          VITE_GOOGLE_APP_ID: ${{ vars.VITE_GOOGLE_APP_ID }}

      - uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    name: Publicar
    needs: build
    runs-on: ubuntu-latest

    environment:
      name: github-pages
      url: ${{ steps.deploy.outputs.page_url }}

    steps:
      - id: deploy
        uses: actions/deploy-pages@v5
